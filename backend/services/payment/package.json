{
  "name": "payment-service",
  "version": "1.0.0",
  "description": "Payment service for Kakraba platform (Mock for development)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "package": "npm run build && cd dist && zip -r ../payment.zip ."
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.400.0",
    "@aws-sdk/client-secrets-manager": "^3.400.0",
    "@aws-sdk/lib-dynamodb": "^3.400.0",
    "stripe": "^14.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/jest": "^29.5.3",
    "@types/node": "^20.4.2",
    "@types/uuid": "^9.0.2",
    "fast-check": "^3.15.0",
    "jest": "^29.6.1",
    "ts-jest": "^29.1.1",
    "typescript": "^5.1.6"
  }
}
